# Project outputs (scoped so we don't ignore all PNG/TSV in the repo)
data/
logs/

# If you prefer to keep dirs but ignore contents:
# /data/*
# !/data/.gitkeep
# /logs/*
# !/logs/.gitkeep

# Binary artifacts produced by the pipeline
*.pkl
*.db

# TensorBoard & projector artifacts
logs/events.out.tfevents*
logs/embedding.ckpt*
logs/checkpoint
logs/sprite.png
logs/metadata.tsv

# Model weights / checkpoints (common patterns)
*.pt
*.pth
*.onnx
*.ckpt

# OS cruft (if you want)
.DS_Store
Thumbs.db__pycache__/
